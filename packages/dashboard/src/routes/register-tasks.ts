import type { ApiRoutesContext } from "./types.js";

export function registerTaskRoutes(_ctx: ApiRoutesContext): void {
  // Superseded by register-task-workflow-routes.ts in FN-2541.
  // Kept temporarily for compatibility with earlier scaffold references.
}
