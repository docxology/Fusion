---
"@fusion/dashboard": patch
---

Enhanced agent screen with full-featured detail view including:
- Agent detail modal with tabs (Dashboard, Logs, Runs, Settings)
- Real-time log streaming when viewing task logs
- Agent health monitoring and status indicators
- Live run cards showing active heartbeat runs
- Agent statistics (total runs, runs today, success rate)
- Improved click-to-open agent detail in both board and list views
