---
"@gsxdsm/fusion": patch
---

Add team-aware Agent Companies import that preserves manager/report hierarchy from companies.sh packages. The CLI and dashboard now resolve TEAM.md and AGENTS.md references into real reportsTo agent IDs instead of manifest paths.
